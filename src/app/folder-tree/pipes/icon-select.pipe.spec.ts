import { IconSelectPipe } from './icon-select.pipe';

describe('IconSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new IconSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
